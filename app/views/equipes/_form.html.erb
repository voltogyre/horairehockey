<%= form_for(@equipe) do |f| %>
  <% if @equipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipe.errors.count, "error") %> prohibited this equipe from being saved:</h2>

      <ul>
      <% @equipe.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :categ_id %><br>
    <%= f.collection_select :categ_id, @categ.all, :id, :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
